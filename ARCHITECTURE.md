# ARCHITECTURE

A map of the code as it stands. `lib/` is Elixir; `native/xqlitenif/src/`
is a Rust crate compiled into a NIF library by Rustler, talking to a
statically linked SQLite through rusqlite. `XqliteNIF` holds nothing but
99 raw NIF stub declarations (every body is `err()`, replaced at load
time by the native function); `Xqlite` wraps most of them with option
validation, result structs and telemetry.

## 1. Module map

Elixir paths are relative to `lib/`, Rust paths to
`native/xqlitenif/src/`.

- `xqlite.ex` — the high-level API: open/close, `query/4`, `execute/4`,
  `stream/4`, the prepared-statement calls, transactions and savepoints,
  the busy policy and observers, `register_progress_hook/3`,
  cancellation, backup/serialize, PRAGMA get/set, schema introspection,
  STRICT-table helpers. It has no wrapper for the update / WAL / commit /
  rollback / log hooks — those are called on `XqliteNIF` directly.
- `xqlite/xqlitenif.ex` — the 100 stubs plus `use RustlerPrecompiled`.
- `xqlite/result.ex`, `explain_analyze.ex` — result structs with
  `from_map/1`; `Result` also implements `Table.Reader`.
  `xqlite/stream_resource_callbacks.ex` and `stream_error.ex` hold the
  `Stream.resource/3` callbacks and the exception they raise;
  `xqlite/pragma.ex` and `pragma_spec.ex` hold 57 typed PRAGMAs with
  `get/4`, `put/4`, validation and per-schema targeting.
- `xqlite/type_extension.ex` and `type_extension/*.ex` — the
  `encode/1` / `decode/1` behaviour, the chain runners, and nine
  built-ins (`Date`, `DateTime`, `NaiveDateTime`, `Time`, `JSON`, `UUID`
  both ways; `Instant`, `Duration`, `Decimal` encode only).
- `xqlite/telemetry.ex`, `telemetry/bridge.ex`,
  `telemetry/open_telemetry.ex` — the compile-gated macros and
  `enabled?/0`, the GenServer re-emitting hook deliveries as
  `[:xqlite, :hook, :*]`, and a dependency-free map to OpenTelemetry
  attribute names.
- `xqlite/schema/*.ex` — the six structs the Rust side builds, plus
  `Types`; `mix/tasks/verify.ex` and `test_seq.ex` — the pre-commit gate
  (cargo runs with cwd set to the crate directory) and the test runner.
- `lib.rs` — atoms, module list, `rustler::init!`; `nif.rs` — all 99
  `#[rustler::nif]` functions, 93 of them `DirtyIo`; `connection.rs` —
  the `XqliteConn` resource (a `Mutex<Option<Connection>>`, the child
  registry, and every hook slot), `with_conn`, `with_conn_mut`,
  open/close, result encoding.
- `statement.rs`, `stream.rs`, `blob.rs` — the three raw-pointer
  resources (a shared `AtomicPtr` plus `with_live_*`),
  `take_and_finalize_raw`, `process_single_step`, the raw binders.
- `session.rs`, `cancel.rs` — `XqliteSession` with its
  leak-rather-than-dangle `close`; `XqliteCancelToken` with the RAII
  `ProgressHandlerGuard`.
- `hook_util.rs` — term helpers, the single-slot `AtomicPtr` lifecycle,
  the copy-on-write `HookList<T>`, `guard_ffi_callback`.
- `progress_dispatch.rs`, `busy_handler.rs`, `wal_hook.rs` — the three
  raw C callbacks: progress ticks and cancel checks, the busy slot, the
  WAL fan-out plus emulated checkpoint.
- `update_hook.rs`, `commit_hook.rs`, `rollback_hook.rs`, `log_hook.rs` —
  master closures installed once, fanning out to a `HookList`; the log
  one is process-wide, installed on the first register, and guards its
  `static` list with a mutex.
- `authorizer.rs` — the one authorizer closure, holding the caller's
  deny-list plus the busy slot's own two rules; `query.rs` — the four `core_*`
  entry points and the two input checks that reject bad SQL; `util.rs` —
  term to `rusqlite::Value` conversion both ways, row encoding,
  identifier quoting, `singular_ok_or_error_tuple`;
  `error.rs` and `constraint_parse.rs` —
  `XqliteError` (43 variants) with `classify_sqlite_error`, and the
  constraint message-text parser.
- `schema.rs`, `explain_analyze.rs`, `pragma.rs`, `transaction.rs` — the
  schema structs, their PRAGMA readers and the column-default
  classifier; the scanstatus and plan collector; PRAGMA name validation;
  the transaction and savepoint SQL, names quoted.

## 2. Data flows

### 2.1 A query

`xqlite.ex:query/4` → a `[:xqlite, :query]` span →
`TypeExtension.encode_params/2`, whose refusal ends the call inside the
span → `nif.rs:query_with_changes` (dirty I/O) →
`connection.rs:with_conn`, which locks the `Mutex` and proves the inner
`Option<Connection>` is `Some`, else `ConnectionClosed` →
`query.rs:core_query_with_changes`, which brackets `core_query` with two
`conn.total_changes()` reads. `core_query` runs `reject_interior_nul`,
prepares, runs `reject_no_statement`, then binds: an empty list binds
nothing, a keyword list goes through
`util.rs:decode_exec_keyword_params`, which refuses a statement of more
than `MAX_NAMED_PARAMETERS` (2 048) parameters before it reads a value
(each atom key gains a leading `:`),
any other list through `decode_plain_list_params`, `nil` means none,
anything else is `ExpectedList`. Both hand each value to
`util.rs:elixir_term_to_rusqlite_value`, where a binary becomes `Text`
when its bytes are valid UTF-8 and `Blob` otherwise, and a map that is an
`%Xqlite.Blob{}` becomes `Blob` whatever its bytes are — any other map,
and `bytes` holding anything but a binary, is refused.
`util.rs:process_rows` encodes each
value with `encode_val` → `encode_f64` / `encode_text` / `encode_blob`.
`nif.rs:encode_query_result_with_changes` builds the map, the Mutex
releases, and `query/4`'s `decode_result_rows/2` decodes the rows and
builds `%Xqlite.Result{}`; a value an extension refuses ends the call
there with `{:error, _}`, after the statement ran. `execute/4` is the same path through
`core_execute`, with the affected count in `Result.changes` and no rows.

### 2.2 Prepared statements

`nif.rs:stmt_prepare` calls `sqlite3_prepare_v2` under `with_conn`,
routing failures through `error.rs:prepare_failure` and snapshotting
column names into the resource. Every later call goes through
`statement.rs:with_live_stmt`, which locks the connection *before*
loading the `AtomicPtr` — that order makes a concurrent finalize safe,
because the finalizer can null the pointer at any moment but cannot call
`sqlite3_finalize` without the same Mutex. Stepping goes through
`stream.rs:process_single_step`, which reads the column count *after* the
step so automatic re-prepare is reflected. `stmt_finalize` and `Drop`
both call `stream.rs:take_and_finalize_raw`: take the connection lock,
swap the shared pointer cell to null, finalize, drop the connection's
registry entry, discard the return code.

### 2.3 Streams

`xqlite.ex:stream/4` → `stream_resource_callbacks.ex:start_fun/1`
(validate `:on_error`, `:batch_size` and `:cancel_tokens`, then
`open_stream/5`: `nif.rs:stream_open`, `stream_get_columns`, and a refusal
of a column list holding a repeated name with
`{:duplicate_column_name, name}` — those last two close the handle they were
handed, while the three option checks run before any handle exists) →
`next_fun/1` → `nif.rs:stream_fetch_cancellable`, which locks the
connection once and steps up to `batch_size` rows in a single hold, growing
the row vector on demand because pre-sizing to an unvalidated `batch_size`
could abort the VM. On exhaustion or error it swaps the pointer to null and finalizes
there and then, replying `:done` or `{:error, reason}` — except that an
error other than a cancellation, after rows were already read, hands those
rows back now and keeps the error for the next fetch. `fetch_batch/1` hands
each batch to `map_rows_to_maps/4`, which decodes each row through
`TypeExtension.decode_row_checked/2` and keys it by column name, and shapes
each element per `:on_error`. A row holding a value an extension refuses
stops the batch there: the rows before it go out, and the refusal waits in
the accumulator's `decode_error` until the next pull answers it per
`:on_error`, as the NIF does with a step error. `after_fun/1` calls the idempotent
`stream_close`. `stream_open` compiles through
`statement.rs:prepare_one`, so SQL holding no statement and SQL holding a
second one are refused at open rather than becoming an empty stream or a
stream over the first statement alone. `build_acc/5` keeps the connection
and the wrapped `:cancel_tokens` in the accumulator, so every fetch
carries the same token list; an empty list makes the cancellable fetch
byte-for-byte the plain one.

### 2.4 Hooks and callbacks

A per-connection master callback is installed exactly once, in
`connection.rs:handle_open_result`, and stays for the connection's life;
`register_*` / `unregister_*` only mutate a `hook_util.rs:HookList<T>`,
which is copy-on-write — writers clone the `Vec` and atomic-swap under
the connection Mutex, the C callback reads one atomic load. The update,
commit and rollback hooks use rusqlite's safe closure API, and the commit
closure always returns `false`, never vetoing. `wal_hook.rs` and
`progress_dispatch.rs` register raw `sqlite3_*` callbacks, since
rusqlite's wrappers cannot carry the state they need, and both run inside
`hook_util.rs:guard_ffi_callback` so a panic returns a neutral value
instead of unwinding into C. Each callback sends with `enif_send` on a
fresh `msg_env`, frees it unconditionally, and rebuilds atoms there.
`log_hook.rs` is process-wide (`sqlite3_config`), so no connection Mutex
is held when it fires; its `static MASTER_LOCK` is taken by the callback
as well as by register/unregister, or the lock-free read would race the
free of the old subscriber vector.

### 2.5 Cancellation

`nif.rs:create_cancel_token` returns a resource holding an
`Arc<AtomicBool>`; `cancel_operation` stores `true` and nothing resets
it. A `*_cancellable` NIF builds a `cancel.rs:ProgressHandlerGuard`
inside the `with_conn` closure — one `CancelSubscriber` per token onto
`progress_dispatch.cancels`, each owning `Arc` held so the raw
`*const AtomicBool` stays valid while reachable. The progress C callback,
installed at open time, fires every `PROGRESS_NUM_OPS` = 8 SQLite VM
instructions, walks `cancels` first, and returns 1 if any flag is set;
SQLite aborts with `SQLITE_INTERRUPT`, which
`error.rs:classify_sqlite_error` maps to `{:error, :operation_cancelled}`.
Dropping the guard unregisters before releasing the `Arc`s. Tick
subscribers share the callback, each with its own counter.

`nif.rs:stream_fetch_impl` is the one that does not use `with_conn`: it
declares the guard after its own connection lock guard and after the
connection is proved open, so reverse declaration order drops the guard
while the Mutex is still held. A cancelled fetch goes through the same
`Err` arm as any other fetch error — swap the pointer, finalize, return —
so the stream is closed and `next_fun/1` emits
`[:xqlite, :cancel, :honored]` with `operation: :stream_fetch` before
routing `{:error, :operation_cancelled}` through the `:on_error` mode.

### 2.6 The remaining flows

The busy slot, the WAL slot, error classification and the telemetry gate
are stated in full as facts in section 4. `nif.rs:backup` and `restore`
are one-shot rusqlite calls under the connection lock;
`backup_with_progress` loops `Backup::step`, checking every cancel token
between steps. `serialize` copies the image into an `OwnedBinary`;
`deserialize` needs `with_conn_mut`. A session is a `Session<'static>`
produced by transmuting away the connection borrow, kept sound by the
`ResourceArc<XqliteConn>` the resource also holds, and every
`sqlite3session_*` call takes the connection Mutex first, the per-session
Mutex second. Two facts live only here: `span_with_stop_metadata` treats
its block's last element as **stop metadata**, so `:stop` measurements
are always just `%{duration, monotonic_time}` and every extra a call
records arrives in the metadata map; and `Xqlite.Telemetry.run_span/3`
emits the three span events itself instead of calling
`:telemetry.span/3`, whose measurements are in the VM's native time
unit while every xqlite measurement is a nanosecond count.

## 3. State machines

### Connection (`connection.rs`)

| State | Event | Next | Function |
|---|---|---|---|
| — | any `open*` NIF | open | `handle_open_result` |
| open | any NIF | open | `with_conn` / `with_conn_mut` |
| open | `close` | closed | `close_connection`: drain the child registry, then `free_handle` closes through `Connection::close()`; a refused close puts the connection back |
| closed | `close` | closed | `close_connection` → `:ok \| {:error, {:database_busy_or_locked, _, _}} \| {:error, {:lock_error, _}}` |
| closed | anything else | closed | `{:error, :connection_closed}` |

`XqliteConn.children` holds one shared pointer cell per prepared
statement, stream and blob xqlite opened on the connection.
`close_connection` finalizes them all under the connection `Mutex` before
dropping the `Connection`, so `sqlite3_close` frees the handle instead of
answering `SQLITE_BUSY`. Statements a virtual-table module owns are not in
the registry — SQLite disconnects those itself during close. A session
registers nothing and is still leaked by an explicit close.

### Prepared statement (`statement.rs`)

A second flag rides alongside the live/final one: whether anything has set
the statement's parameters. It starts false for a statement that takes one or
more and true for one that takes none, and only a step reads it. A third
piece of state is not the library's: SQLite's own `sqlite3_stmt_busy`, which
`stmt_clear_bindings` reads to refuse a clear that would null the rest of a
run in progress.

| State | Event | Next | Function |
|---|---|---|---|
| — | `stmt_prepare` | live, parameters unset (set when the statement takes none) | `nif.rs:stmt_prepare` reads `sqlite3_bind_parameter_count` beside the column count |
| live, parameters unset | a step of any kind | unchanged | `{:error, {:parameters_unbound, %{expected: n}}}`, behind the liveness check |
| live | a bind that succeeded | live, parameters set | `nif.rs:stmt_bind` |
| live | a bind the library refused (the count, the names, a value it cannot convert, a value over the connection's length limit) | unchanged | the whole list is judged before the first bind, so nothing was bound and an earlier successful bind stays in force |
| live | a bind SQLite refused after it had taken values | live, parameters unset | the values before the failing one stayed bound and that one is NULL, which is no state to run: `nif.rs:stmt_bind` clears the flag inside the same lock. The length check before the bind loop leaves an allocation failure as the only way here |
| live | `stmt_clear_bindings` | live, parameters set | every parameter is NULL on purpose |
| live, mid-run, the statement takes parameters | `stmt_clear_bindings` | unchanged | `{:error, :statement_mid_run}`: `sqlite3_clear_bindings` has no mid-run check of its own, so the clear would release the values in place and leave every row still to come reading NULL. `sqlite3_stmt_busy`, read under the same lock as the clear, is the tell: non-zero exactly while a step has run and neither `SQLITE_DONE` nor a reset has followed. A statement that takes no parameters has nothing to release and is cleared as anywhere else |
| live | `stmt_reset` | unchanged | SQLite keeps the bindings across a reset |
| live | bind / reset / clear | live | `with_live_stmt` |
| live | `stmt_step` → `{:row, _}` | live | `process_single_step` |
| live | `stmt_step` → `:done` | live | auto-resets on next step |
| live | a step reads a row it cannot decode, after other rows of the batch | live, error held | `stmt_multi_step_impl` hands back the rows with `done: false` and keeps the error in `XqliteStatement.pending_error` |
| live, error held | `stmt_step`, `stmt_multi_step` or its cancellable twin | live | answer the held error and empty the slot, before stepping; the statement carries on at the row after the bad one |
| live, error held | `stmt_reset` | live | empty the slot: the run it belonged to is over |
| live, error held | `stmt_finalize` | final | empty the slot and answer the lifecycle result |
| live | a step that fails outright (busy, I/O, a runtime error in the SQL, a trigger's `RAISE`, a cancellation) | live | `StepFailure::Failed`: answer at once, discard the batch's rows, hold nothing; SQLite reruns from the top on the next step |
| live | `stmt_finalize` or GC | final | `take_and_finalize_raw` |
| live | the connection is closed | final | `close_connection` drains the registry |
| final | any step or bind | final | `{:error, :statement_finalized}` |
| final | `stmt_column_names` | final | prepare-time snapshot |
| final | `stmt_finalize` | final | `:ok` |

### Stream (`stream.rs`, `stream_resource_callbacks.ex`)

| State | Event | Next | Function |
|---|---|---|---|
| — | `stream_open` | open | `nif.rs:stream_open` |
| — | `stream_open` with no statement, or with a second one | — | refused by `statement.rs:prepare_one`, no handle |
| open | `stream_fetch` → rows | open | `nif.rs:stream_fetch` |
| open | `stream_fetch` exhausts | closed | swap + finalize in place |
| open | `stream_fetch` errors with no row of that batch read | closed | swap + finalize, then error |
| open | `stream_fetch` errors after reading rows | closed, error held | swap + finalize, hand back the rows, keep the error in `XqliteStream.pending_error`; both failure kinds are held back here, the statement being finalized already |
| closed, error held | `stream_fetch` | closed | answer the held error and empty the slot, before any step |
| open | `stream_fetch_cancellable` with a signalled token | closed | swap + finalize, discard the batch's rows, then `{:error, :operation_cancelled}` |
| any | `stream_close` or GC | closed | `take_and_finalize_atomic_stmt`, which empties the held error first |
| open | the connection is closed | closed | `close_connection` drains the registry |
| closed | `stream_fetch` | closed | `:done` |

`stream_fetch` proves the connection open before it reads the statement
pointer: after a close both are gone, and the caller must still hear
`{:error, :connection_closed}` rather than `:done`. It then looks for a
held error, before the batch loop — a finalized statement's null pointer
would otherwise answer `:done` and the stream would look complete.

The busy slot's own transitions are in section 4
(`busy-slot-policy-single-observers-many` and
`busy-slot-restores-displaced-timeout`): the slot moves between empty,
policy, observers and both, restoring the displaced `busy_timeout` on
every transition into empty and recording the current one on the way out.
Both edges also re-install the connection's authorizer, since the held
slot adds rules of its own; a take that then fails puts the flag back and
re-installs again.

Blobs share the stream's two-state shape, closing on the connection's
close as well; a session does not, and an explicit close leaks it. A poisoned
`Mutex` is terminal everywhere: `{:error, {:lock_error, _}}`.
`nif.rs:txn_state` maps rusqlite's `TransactionState` to `:none`,
`:read`, `:write`, or `:unknown`; `transaction_status/1` and
`autocommit/1` are its two boolean views.

## 4. Shared facts with consumers

Facts more than one place in the tree relies on. For every row, the full
consumer list and the `rg` / `ast-grep` patterns that regenerate it live
in the review registry, in the workdir that `AGENTS.md` names. Paths
below are relative: Elixir to `lib/`, Rust to `native/xqlitenif/src/`,
pins to `test/`.

| id | statement | producer | pin |
|---|---|---|---|
| `changes-reported-on-total-delta` | query_with_changes reports sqlite3_changes() only when sqlite3_total_changes() moved across the statement, and 0 otherwise; sqlite3_changes() itself is sticky and survives SELECT, DDL and PRAGMA. | `query.rs:core_query_with_changes` | `nif/query_with_changes_test.exs: "DDL after DML returns changes 0 (no sticky leak)"` |
| `conn-mutex-covers-every-sqlite-call` | Every sqlite3_* C call runs with the connection Mutex held for its whole duration; an AtomicPtr swap gives pointer ownership, never connection access. The cancel-token guard a fetch registers is dropped before the connection lock is released, because the hook list frees the old subscriber vector right after its atomic swap while the C progress callback reads that vector without a lock. | `connection.rs:with_conn` | unpinned |
| `cancel-check-every-8-vm-ops` | The progress handler fires every 8 SQLite VM instructions, which bounds cancellation latency and sets the unit that a progress hook's every_n counts. | `progress_dispatch.rs:PROGRESS_NUM_OPS` | `nif/stream_cancel_test.exs: "a table scan reports the cancel within three single-row fetches"` |
| `cancel-token-single-use` | A cancel token's flag is set once and never reset; a cancellable call takes a list of tokens and any set token aborts it. A stream hands its cancel_tokens to every fetch, so a signalled token ends that stream and every later stream it is handed to. | `cancel.rs:XqliteCancelToken::cancel` | `nif/statement_cancel_test.exs: "an already-signalled token cancels before any stepping"; nif/stream_cancel_test.exs: "after a cancel the connection is clean and the token stays spent"` |
| `wal-hook-and-autocheckpoint-share-one-slot` | Holding the WAL hook disables SQLite's built-in autocheckpoint, so the master callback runs the passive checkpoint itself from a threshold that starts at SQLite's own default of 1000 pages; set_pragma reinstalls the callback and get_pragma reports the emulated threshold. Both comparisons match the pragma name without regard to ASCII case (eq_ignore_ascii_case), so a raw PRAGMA WAL_AUTOCHECKPOINT in any spelling is repaired and reported, not only the lower-case spelling. | `wal_hook.rs:wal_hook_callback`, `wal_hook.rs:DEFAULT_WAL_AUTOCHECKPOINT_PAGES` | `nif/wal_hook_test.exs: "emulated autocheckpoint defaults to SQLite's stock 1000 pages"` |
| `busy-slot-policy-single-observers-many` | One retry policy (replaced on re-set) plus any number of observer pids share one C callback; observers fire whether or not a policy is installed. What the slot holds is mirrored in BusySlotFlags (held, policy, observer count), which the connection's authorizer closure and the error mapping read without touching the slot pointer. | `busy_handler.rs:BusySlotState`, `busy_handler.rs:BusySlotFlags` | `nif/busy_handler_test.exs: "re-setting the policy replaces the previous one"` |
| `busy-slot-restores-displaced-timeout` | Taking the busy slot records the current PRAGMA busy_timeout through xqlite's own read, which passes the caller's authorizer (the composed closure is installed first, then the read runs under the internal-read flag); a read that fails for any other reason refuses the take and leaves the slot as it was, never remembering 0. Emptying the slot puts the recorded value back, and a policy-less slot emulates the timeout with SQLite's own retry-delay schedule. | `busy_handler.rs:swap_in`, `busy_handler.rs:take_slot` | `nif/busy_handler_test.exs: "unregistering the last observer restores the busy_timeout"; "an observer registered under a :pragma deny keeps the connection's wait"; "a policy set under a :pragma deny gives the wait back when it is removed"` |
| `busy-timeout-fits-c-int` | A busy_timeout above c_int::MAX (2 147 483 647 ms) is refused with {:cannot_execute, reason}, never clamped, on busy_timeout/2, the open options and set_pragma/3; busy_timeout/2 refuses any term that is not a non-negative integer the same way, before anything reaches SQLite. | `busy_handler.rs:busy_timeout_c_int` | `nif/busy_handler_test.exs: "busy_timeout refuses a value past SQLite's 32-bit limit"` |
| `busy-timeout-write-refused-while-slot-held` | While the busy slot holds a policy or an observer, a statement writing busy_timeout — any spelling of the PRAGMA, XqliteNIF.set_pragma/3, the typed Xqlite.set_pragma/3 — is refused as it is prepared with {:busy_timeout_write_refused, %{policy, observers}}, never the generic authorization denial, on every path that prepares SQL (query, execute, execute_batch, prepare, stream, a statement or stream from before the take, a session); a value of 0 or less is refused the same way; reading PRAGMA busy_timeout stays allowed and reads 0 while the slot is held; Xqlite.busy_timeout/2 is the way to change the wait; with the slot empty the write is accepted. | `authorizer.rs:decide`, `authorizer.rs:busy_timeout_action`, `connection.rs:with_busy_timeout_rule` | `nif/busy_handler_test.exs: "a raw busy_timeout write is rejected while an observer holds the slot"` |
| `extended-code-masking` | Error classification masks the extended code with & 0xFF and compares against SQLite's C constants, because rusqlite's ErrorCode enum values differ from them. | `error.rs:classify_sqlite_error` | unpinned |
| `error-shapes-per-class` | Each error class has a fixed Elixir shape: bare atoms for lifecycle and input errors (:connection_closed, :statement_finalized, :statement_mid_run for a clear_bindings on a statement mid-run that takes parameters, :operation_cancelled, :multiple_statements, :null_byte_in_string); {tag, extended_code, message} for busy/locked, read-only, schema-changed and authorization-denied; {:constraint_violation, kind, details}; the catch-all {:sqlite_failure, code, extended_code, message}; and {:sql_input_error, %{code, message, sql, offset}} when a SqlInputError would otherwise classify generically.; {:busy_timeout_write_refused, %{policy, observers}} for xqlite's own authorizer rule. Also, made in Elixir, {:type_extension_refused, map}, {:read_only_pragma, atom}, {:invalid_pragma_value, map}, {:invalid_cancel_tokens, list_refusal}, {:unknown_pragma, the caller's atom or string}, {:invalid_pragma_name, the caller's key when it is neither an atom nor a string, nil (no name on the raw doors although it is an atom), or the name's bytes exactly as given when the native check refuses them: empty, not UTF-8, or holding a byte outside A-Z, a-z, 0-9 and _}, {:not_a_plain_table, %{table, type}}, :transaction_in_progress and {:rowid_shadowed, name}; and, made in Rust, {:invalid_blob_bytes, %{position, type}}. {:unsupported_data_type, atom} names the term kind the binder cannot store, :bitstring for a bitstring that is not a whole number of bytes on the plain path as on the wrapper; {:invalid_cancel_tokens, list_refusal} is answered by every cancellable Xqlite door and by the raw token-list NIFs for every wrong token argument — :bad_element with the one-based position and the element's type, :improper_tail, and :not_a_list on the raw NIFs (cancel.rs:decode_tokens re-tags the walk's refusal), where the Elixir doors read a bare term as one token and answer :bad_element at position 1; cancel_operation/1 takes one token and refuses a list as the element at position 1 — so {:expected_list, _} on a cancellable call is always about the parameters. Also made in Elixir: {:invalid_pragma_argument, %{pragma, value, reason}} from the pragma getters and the setter's options (reason :invalid_options for options that are no keyword list, an unknown key, or a :db_name that is neither a string nor an atom, value the whole term or the offending pair; reason :invalid_utf8 for an argument or a :db_name whose bytes are not UTF-8, value the bytes as given) and {:invalid_hook_option, %{key, value, reason}} from register_progress_hook/3; a PRAGMA value handed to set_pragma answers :null_byte_in_string for a NUL byte, :invalid_utf8_in_string for bytes that are not UTF-8 and {:unsupported_data_type, :bitstring} for a partial bitstring; every text argument of the raw NIFs but the two pragma names (41 arguments over 35 NIFs, read through util.rs:TextArg and MaybeTextArg) answers the bare atom :invalid_utf8_in_string for a binary that is not UTF-8 and still raises ArgumentError for a term that is no binary. {:cannot_execute_pragma, name, reason} carries the bare pragma name in every arm. {:missing_parameter, %{index, name}} for a statement parameter a keyword list left out and {:duplicate_parameter_name, name} for two keys resolving to one parameter, name being the resolved name, made in Rust on every named door; {:invalid_batch_size, %{provided, minimum: 1}} carries the caller's term as given on the statement and the stream doors alike, and Xqlite.stream/4 answers it at the call for a batch size outside 1..2^63-1; {:parameters_unbound, %{expected}} answers a step on a statement with parameters that no bind has set since prepare; {:integer_out_of_range, %{position}} (the map empty on the raw PRAGMA setter) answers an integer past the signed 64-bit range where {:cannot_convert_to_sqlite_value, text, text} used to — that tag is retired — and {:value_too_large, %{byte_size, limit}} answers a TEXT or BLOB parameter longer than the connection's length limit (sqlite3_limit(SQLITE_LIMIT_LENGTH), one billion bytes unless lowered, never above c_int::MAX) on every door that binds — raw and rusqlite alike — before any value is bound, and the object name XqliteNIF.get_create_sql/2 looks up (schema.rs:create_sql), judged by the same limit before the lookup runs, limit being that connection's limit and nothing truncated, while SQLite's own SQLITE_TOOBIG, which still answers a step whose row, concatenation or column read exceeds the same limit, is {:too_big, code, message}, the shape family of :read_only_database; {:invalid_limit_category, term} and {:invalid_limit_value, %{category, value}} answer get_limit/2 and put_limit/3, the value judged before the category (an integer outside 0..2_147_483_647, bignums included, at the Xqlite guard and in limits.rs:judge_value); {:too_many_named_parameters, %{count, limit}}, made in Rust, answers a keyword list on a statement with more than 2 048 parameters before any value is decoded; {:invalid_type_extensions, list_refusal} answers a :type_extensions option that is no proper list of extension modules — an atom that names no extension module is :bad_element at its position — on every Xqlite door that takes it, before any metadata or NIF call. {:invalid_open_option, %{key: nil, reason: :not_a_pair, value}} answers an element of the options list that is not a {key, value} pair, and a list whose tail is not [] with the tail as value; options that are not a list at all raise from the openers' guards. {:expected_list, map}, {:expected_keyword_list, map} and {:expected_keyword_tuple, map} carry a reason and the BEAM type of the term that stopped the walk (the keyword-tuple one always :bad_element with the element's position); the same map, made in Rust, is the raw token-list NIFs' {:invalid_cancel_tokens, _}, with all three reasons; the read-side Xqlite.TypeExtension.decode_rows/2 answers {:expected_list, map} for a rows term that is no list, an outer tail that is not [], a row that is no list, and a row whose tail is not [] when an extension walk runs, never raising. {:duplicate_column_name, name}, made in Elixir by Xqlite.stream/4 at open, names the first column name that repeats in SQLite's order. | `error.rs:MissingParameter`, `error.rs:DuplicateParameterName`, `error.rs:Encoder`, `error.rs:encode_pragma_name`, `error.rs:classify_sqlite_error` | `nif/authorizer_test.exs: "denying :delete blocks DELETE but leaves SELECT working"; integer_range_law_test.exs: "the anchor: a number one past the top is refused by its position", "the raw PRAGMA setter answers without a position", "nothing is written when a value in the middle is out of range"; value_length_limit_law_test.exs: "the anchor: one byte over the limit is refused and nothing is written", "a value of exactly the limit binds, and the row it builds does not fit", "a BLOB is judged by its bytes like a TEXT value"; nif/statement_test.exs: "a row longer than the limit is SQLite's own refusal at the step"` |
| `constraint-details-map` | A constraint violation carries %{message, table, columns, index_name, constraint_name, source_type, target_type}, with nil for anything the SQLite message did not name. | `constraint_parse.rs:parse_details` | `strict_table_test.exs: "converts clean table to STRICT"` |
| `message-text-parsing-is-confined` | Classification reads SQLite message text in exactly two places: constraint_parse.rs, and the four name-prefix arms in classify_sqlite_error (no such table, no such index, table ... already exists, index ... already exists). The four arms only decide which prefix applies; the reading itself is done by three helpers beside them — strip_ascii_prefix, name_after and name_between — and name_between also strips the trailing " already exists". | `error.rs:classify_sqlite_error` | `nif/execution_test.exs: "execute/3 returns error for NoSuchTable on INSERT"` |
| `hook-message-shapes` | Subscribers receive eight tuple shapes: {:xqlite_update, action, db, table, rowid}, {:xqlite_wal, db_name, pages}, {:xqlite_commit}, {:xqlite_rollback}, {:xqlite_busy, retries, elapsed_ms}, {:xqlite_progress, count, elapsed_ms} (or with a tag second), {:xqlite_log, code, message}, and {:xqlite_backup_progress, remaining, pagecount}. The telemetry bridge re-emits the first seven; backup progress is not bridged. | `update_hook.rs:send_update_to_pid`, `wal_hook.rs:send_wal_to_pid`, `commit_hook.rs:send_commit_to_pid`, `rollback_hook.rs:send_rollback_to_pid`, `busy_handler.rs:send_busy_to_pid`, `progress_dispatch.rs:send_tick_to_pid`, `log_hook.rs:send_log_to_pid`, `nif.rs:send_backup_progress` | `nif/update_hook_test.exs: "delivers {:xqlite_update, :insert, ...} on INSERT"` |
| `hook-handles-are-opaque-and-idempotent` | Every register_* returns a u64 handle unique within its own list; unregistering an unknown or already-removed handle is a no-op that still answers :ok. | `hook_util.rs:HookList::register`, `hook_util.rs:HookList::unregister` | `nif/wal_hook_test.exs: "register / unregister returns handle and is idempotent"` |
| `type-extension-chain-first-match-wins` | encode/1 and decode/1 answer {:ok, value}, :skip or {:error, reason} (type-extension-callbacks-answer-three-shapes); the chain is walked in list order, the first {:ok, _} wins and the rest are not consulted, and unmatched values pass through unchanged. An encoder may answer {:error, reason}: the chain stops and the call answers {:type_extension_refused, %{position, extension, reason}} inside its telemetry span; encode_params/2 answers {:ok, params} or that tuple and accepts nil; the chain runs on every parameter-taking Xqlite function (bind/3, explain_analyze/4 and the three cancellable forms included) and decodes rows on every function that returns a result map; step/1, multi_step/2 and multi_step_cancellable/3 rows stay raw. encode_params/2 and decode_rows/2 judge their extension list through the shared walk first and answer {:invalid_type_extensions, refusal} for a list that fails it — decode_rows/2 answers {:ok, rows} or that error — while encode_value/2 and decode_value/2 walk nothing and their docs say the caller vouches for the list. | `xqlite/type_extension.ex:encode_value/2`, `xqlite/type_extension.ex:decode_value/2` | `type_extension_test.exs: "first matching extension wins"; type_extension_test.exs: "the list is judged before any value is touched", "a module with both callbacks but no declaration is refused by both"` |
| `default-value-classification` | A column default arrives as :none, {:literal, v}, {:blob, bytes}, {:current, :time \| :date \| :timestamp}, or {:expr, sql}; nothing is constant-folded, and an integer past 64 bits or a non-finite float falls back to {:expr, _}. | `schema.rs:classify_default` | `schema_default_value_test.exs: "the full default-value matrix classifies as designed"` |
| `non-finite-floats-become-atoms` | A REAL that is not finite reads back as :positive_infinity or :negative_infinity, and NaN as nil, because the BEAM cannot hold a non-finite double. | `util.rs:encode_f64` | `nif/query_test.exs: "query/3 reads non-finite floats as sentinel atoms and stays usable"` |
| `blob-encoding-threshold-64-bytes` | On the query path a BLOB larger than 64 bytes comes back as a zero-copy resource binary and one of 64 bytes or fewer is copied into a process-heap binary; the stream and blob_read paths always copy. | `util.rs:HEAP_BINARY_THRESHOLD`, `util.rs:encode_blob` | unpinned |
| `sql-input-rejections` | SQL is refused, never truncated or half-run: an interior NUL byte is :null_byte_in_string on every entry point; on query, execute, prepare, stream and explain_analyze, SQL holding no statement (empty, whitespace, comments, bare semicolons) is {:cannot_execute, "SQL contains no statement"} and a real second statement after the first is :multiple_statements, while trailing whitespace, comments and semicolons pass — one rule, statement.rs:prepare_one. When the text after the first statement is not itself a valid statement, the tail is re-compiled and the caller gets the compiler's own error with the tail as the SQL in it, a {:sql_input_error, _}, not :multiple_statements. The NUL rule holds for a PRAGMA value too: a value handed to set_pragma with an interior NUL byte is :null_byte_in_string before any statement is built (util.rs:pragma_text), for the same reason — SQLite's tokenizer would stop at the NUL and run a shorter statement than was built. A binary that is not UTF-8 where text is meant — SQL text, a path or URI, a savepoint, table, index, object, schema or column name, a hook tag, an extension entry point — is :invalid_utf8_in_string on every raw NIF (41 text arguments over 35 NIFs read through util.rs:TextArg and MaybeTextArg, one validation, no copy beyond rustler's own; the pragma name on get_pragma and set_pragma is read as raw bytes instead and judged by pragma.rs:validate_name, answering {:invalid_pragma_name, bytes}), refused before SQLite is asked anything; a term that is no binary still raises. A parameter name that is not UTF-8 never reaches a walk: the SQL text holding it is refused first, so rusqlite's parameter_name, whose body expects UTF-8, is unreachable — pinned. Text holding no statement is told on the rusqlite doors by the null statement's marks — read-only, no columns, no parameters, no expansion — and the expansion is read with the connection's length limit lifted to its ceiling and restored around the read (limits.rs:with_length_limit_lifted), so an absent expansion means a null statement at any limit and any text length, and both door families answer the same for a comment-only text; the raw doors see the null pointer itself. | `query.rs:reject_interior_nul`, `query.rs:reject_no_statement`, `statement.rs:prepare_one` | `nif/error_input_test.exs: "interior NUL in SQL text is rejected on query/execute/execute_batch", "SQL text that is not UTF-8 is refused on every SQL door", "an interior NUL and bytes that are no UTF-8 answer their own atom", "every text argument of the raw NIFs refuses bytes that are no UTF-8", "every text argument still raises for a term that is no binary"; parameter_coverage_law_test.exs: "SQL holding a parameter name that is no UTF-8 is refused at the door"; nif/statement_test.exs: "a lowered length limit does not hide a statement", "a lowered length limit does not hide a read-only statement", "a comment-only text holds no statement at any limit or door"` |
| `batch-size-and-step-count-floors` | A batch size below 1 is {:invalid_batch_size, %{provided: v, minimum: 1}} and pages_per_step below 1 is {:invalid_pages_per_step, v}; neither is clamped. For every_n the Elixir door Xqlite.register_progress_hook/3 answers {:invalid_hook_option, %{key: :every_n, value: v, reason: :invalid_value}} for a value that is not a positive integer, before the NIF is reached, while the raw NIF still answers {:cannot_execute, _} for 0. The two :invalid_batch_size producers agree: provided is the caller's term as given; the stream fetch doors take a term and answer for a wrong type, the statement doors take an i64 and raise for one; Xqlite.stream/4 judges :batch_size at the call — an integer in 1..2^63-1, the fetch door's own domain — and answers the same refusal; Xqlite.multi_step/2 keeps its integer guard. On the statement batch doors the refusals come in the order cancel tokens (on the cancellable twin), batch size, then finalized or closed, then parameters_unbound, pinned. | `xqlite/stream_resource_callbacks.ex:validate_batch_size/1`, `nif.rs:stmt_multi_step_impl`, `nif.rs:stream_fetch_impl`, `nif.rs:register_progress_hook`, `nif.rs:backup_with_progress` | `nif/statement_test.exs: "multi_step rejects a batch size below one"; nif/stream_test.exs: "stream_fetch/2 reports a float batch size as the caller wrote it"; xqlite_test.exs: "stream/4 rejects a batch size that is not a positive integer at open", "stream/4 rejects a batch size past the fetch door's range at open", "stream/4 takes the largest batch size the fetch door reads"; nif/statement_test.exs: "the batch doors judge the batch size before anything else"` |
| `stream-on-error-modes` | :raise (the default) yields row maps and raises Xqlite.StreamError; :halt yields row maps, logs the reason and stops; :emit_error yields {:ok, row} then one terminal {:error, reason}; an unknown value is {:error, {:invalid_on_error, value}} at open. A cancelled fetch, {:error, :operation_cancelled}, is one more error routed through the modes. A step error other than a cancellation ends the batch early: the rows read before it are delivered as a normal batch and the error answers the next fetch; with no rows read it answers at once; a cancellation still discards the batch; a closed stream drops any kept error and answers :done. | `xqlite/stream_resource_callbacks.ex:handle_fetch_error/2`, `xqlite/stream_resource_callbacks.ex:validate_on_error/1` | `xqlite_test.exs: "stream/4 rejects an unsupported :on_error mode at open"` |
| `statement-column-names-fall-back-after-finalize` | stmt_column_names reads live column metadata so automatic re-prepare is reflected, and serves the prepare-time snapshot only when the statement is finalized or the connection is closed. | `nif.rs:stmt_column_names` | `nif/statement_test.exs: "operations after finalize report :statement_finalized; names stay cached"` |
| `authorizer-validates-the-whole-list-first` | An unrecognised action atom returns {:invalid_authorizer_action, atom} and installs nothing; the authorizer is a single slot that a second call replaces, and the connection carries one composed closure: the caller's denied kinds plus the busy slot's two rules while the slot is held (a busy_timeout write refused, xqlite's own busy_timeout read allowed); removing the caller's list while the slot is held keeps those rules, and emptying the slot with no list clears the authorizer. | `authorizer.rs:parse_denied`, `authorizer.rs:sync` | `nif/authorizer_test.exs: "unrecognized atom is a structured error and installs nothing"` |
| `telemetry-is-compile-time-gated` | Telemetry is gated by the compile-time :telemetry_enabled flag, false by default: enabled?/0 is a compile-time constant, the macros expand to no :telemetry call when off, and both bridge constructors then return {:error, :telemetry_disabled}. | `xqlite/telemetry.ex:enabled?/0` | `telemetry_test.exs: "enabled?/0 reflects the compile_env value"` |
| `result-struct-and-table-reader` | %Xqlite.Result{} carries columns, rows, num_rows and changes, and implements Table.Reader as {:rows, %{columns: _, count: _}, rows}. | `xqlite/result.ex` | `result_test.exs: "Table.Reader returns rows with metadata"` |
| `open-applies-a-fixed-pragma-set` | Xqlite.open/2 and open_in_memory/1 validate options against one NimbleOptions schema, then apply exactly the nine PRAGMAs in @pragma_order with busy_timeout first; an unknown key is {:invalid_open_option, %{reason: :unknown_key, ...}}, and the read-only and temporary openers apply none. Every value the open path hands the NIF passes Xqlite.Pragma.check_value/2, so an integer past what SQLite stores (busy_timeout, cache_size, wal_autocheckpoint) is refused at open instead of reading back 0. The openers guard their options with is_list/1 (a non-list raises FunctionClauseError, the documented kind); an element that is not a {key, value} pair, or a tail that is not [], answers {:invalid_open_option, %{key: nil, reason: :not_a_pair, value: the element or the tail}} before any option is validated, from a hand-written walk that never raises from Enum. | `xqlite.ex:apply_pragmas/2`, `xqlite.ex:@pragma_order` | `open_opts_test.exs: "rejects unknown option"` |
| `db-path-is-nil-without-a-file` | In-memory and temporary databases report {:ok, nil} from db_path; SQLite's empty filename is normalised away. | `nif.rs:db_path` | `nif/connection_test.exs: "db_path returns nil (no backing file)"` |
| `api-armor-and-threadsafe-are-compiled-in` | The bundled SQLite always reports ENABLE_API_ARMOR and a THREADSAFE= entry in PRAGMA compile_options; nothing in this crate's configuration can turn either off. | `native/xqlitenif/Cargo.toml:rusqlite-bundled-feature` | `nif/connection_test.exs: "compile_options returns known flags"` |
| `bare-ok-shares-one-encoder` | Every NIF whose success carries no value encodes its result through one helper, so success is the bare atom :ok (never {:ok, _}) and failure is {:error, reason} drawn from the same structured reason set. | `util.rs:singular_ok_or_error_tuple` | `nif/connection_test.exs: "close is idempotent"` |
| `identifiers-are-double-quoted` | Identifiers that reach SQL as text are wrapped in double quotes with embedded double quotes doubled, by quote_identifier in Rust and Xqlite.Pragma.quote_name/1 in Elixir; string values are bound as parameters, or, where SQL forbids a parameter (PRAGMA), quoted with the quote doubled by Xqlite.Pragma.format_pragma_value/1. | `util.rs:quote_identifier`, `xqlite/pragma.ex:quote_name/1` | `nif/transaction_test.exs: "isolated: savepoint with double quotes in name"` |
| `strict-rebuild-renames-by-token` | enable_strict_table/2 rewrites the stored CREATE TABLE statement by its own name token, whatever its quoting style (double quotes, backticks, brackets, bare), never compiles the caller's name into a pattern, converts a table twice without failing, and refuses a WITHOUT ROWID table with {:without_rowid_unsupported, table}. A saved index or trigger statement is replayed by putting "<schema>". in front of its stored name token, never over it — SQLite stores index and trigger SQL without the qualifier, without IF NOT EXISTS and without TEMP, so the replay stores the same bytes again and CREATE TEMP TRIGGER beside a qualifier is never emitted. | `xqlite.ex:rebuild_as_strict/4`, `xqlite.ex:qualify_object_sql/2` | `strict_table_test.exs: "a table stored double-quoted converts", "every index shape comes back byte-identical", "a trigger on the table survives and still fires", "a temporary trigger on a main table survives and still fires", "a table in an attached database keeps a usable index"` |
| `close-finalizes-children` | close finalizes every prepared statement, stream and blob still open on the connection under the Mutex first, then frees the SQLite handle through Connection::close() and passes SQLite's own refusal on as {:database_busy_or_locked, code, message} instead of dropping it (the connection goes back into its slot and can be closed again; no state the library can reach makes SQLite refuse today); a WAL database's -wal file is gone after close; a child's later operation answers :connection_closed and its finalize or close is :ok; a second close is :ok; sessions are not covered. | `connection.rs:close_connection` | `nif/connection_test.exs: "an unstepped prepared statement does not keep the handle open"; connection.rs: tests::a_refused_close_answers_the_classified_error` |
| `telemetry-units-are-nanoseconds` | Every duration and timestamp measurement xqlite emits (duration, monotonic_time, system_time, wall_time_ns, total_duration, elapsed) is an integer nanosecond count from System.monotonic_time(:nanosecond) or System.system_time(:nanosecond), on spans and on point events alike; counts such as rows_returned are not durations. | `xqlite/telemetry.ex:run_span/3`, `xqlite/telemetry.ex:monotonic_time/0` | `telemetry_test.exs: ":stop reports duration and monotonic_time as nanoseconds"` |
| `telemetry-events-have-one-catalogue` | Xqlite.Telemetry.events/0 is the event catalogue (35 entries, 14 of them spans): a source census test proves every emit and span literal in lib/ is in it, drift tests prove the moduledoc and the guide list exactly it, and a reachability test proves every entry fires. The telemetry guide's metadata column lists every start-metadata key except the common conn and sql (stop-only keys may be named), and a census test keeps the column in step with the module doc's maps. | `xqlite/telemetry.ex:events/0` | `telemetry_test.exs: "every emission site in lib/ spells out a catalogued event"` |
| `poisoned-connection-lock-is-never-recovered` | A poisoned connection Mutex is never recovered: every conn.lock() maps the poison error to LockError and the connection is abandoned (close included), because after a panic SQLite's own state may be half written and must not be touched; a poisoned child-map lock makes close return before the handle is taken, so that connection stays open and usable and every later close repeats the same error. into_inner() appears only on the process-global log registry lock, the per-session guard and the pending-error slots of the stream and of the statement, never on a connection lock. | `connection.rs:with_conn`, `connection.rs:with_conn_mut` | unpinned |
| `stream-close-reason-is-the-outcome` | The [:xqlite, :stream, :close] event's reason is the stream's own outcome — :drained when every row was read, :halted when the consumer stopped early, :errored when a fetch failed in any :on_error mode — never the result of the close call; a failed close adds close_error to the metadata instead. | `xqlite/stream_resource_callbacks.ex:after_fun/1` | `stream_close_reason_law_test.exs: "an error stream in :emit_error mode closes with :errored"` |
| `pragma-accessors-refuse-unknown-names` | The typed doors Xqlite.Pragma.get/2,3,4 and Xqlite.Pragma.put/3,4 refuse a name outside the typed schema with {:error, {:unknown_pragma, name}} carrying the caller's own atom or string, before any PRAGMA statement is built (SQLite would otherwise parse and ignore the unknown pragma and report success). The raw doors Xqlite.get_pragma/2 and Xqlite.set_pragma/3 resolve a name the schema knows the same way, but hand a name outside the schema to SQLite as written, and a read of a name SQLite ignores answers {:ok, :no_value}, the same atom every read door answers for a PRAGMA the connection has no row for (mmap_size on a database that is not a file, legacy_file_format and incremental_vacuum everywhere), a member of get_result/0 that no write door accepts. Every door answers {:invalid_pragma_name, key} for a key that is neither an atom nor a string, the raw doors answer {:invalid_pragma_name, nil} for nil before any conversion (the typed doors {:unknown_pragma, nil}; true and false stay names SQLite ignores), and the raw doors answer it with the name string itself when the native check refuses that string (empty, or a byte outside A-Z, a-z, 0-9 and _). The fold is ASCII (String.downcase/2 with :ascii), as SQLite folds, for atoms and strings alike. After the name, the options are judged once — the last argument, merged with the third when that is a keyword list: a keyword list whose only key is :db_name with a string, an atom or nil, anything else answering {:invalid_pragma_argument, %{pragma, value, reason: :invalid_options}} with the whole term or the offending pair as value, on put/4 after check_value/2 — and then the getters judge the argument position against what the PRAGMA reads with: a scalar (string, atom other than nil, integer) is the argument, a keyword list ([] included) is the options, and any other list, nil, a missing argument on a PRAGMA that reads only with one, or an argument on a PRAGMA with no one-argument read form answers {:invalid_pragma_argument, %{pragma, value, reason}} (reason :not_a_scalar, :missing or :takes_no_argument) before any statement is built — so a getter never builds the PRAGMA name(value) form SQLite reads as a write. An integer argument is written into the statement as a number and a binary or atom as a quoted name, so the number-reading pragmas (integrity_check, quick_check, optimize, incremental_vacuum) take their number through the getter. A binary whose bytes are not UTF-8 in the argument position or as a :db_name answers {:invalid_pragma_argument, %{pragma, value, reason: :invalid_utf8}} on the typed doors before anything is built (a NUL byte keeps :null_byte_in_string a step later); on the raw doors the name is judged as bytes by the native check and handed back as given, and a raw setter value that is not UTF-8 answers :invalid_utf8_in_string. The raw reader Xqlite.get_pragma/2 (XqliteNIF.get_pragma/2, pragma.rs:get through query_row) answers the first row's first column, so a PRAGMA that answers several rows is cut to its first; the typed reader knows the sixteen list-answering PRAGMAs and answers the list. | `xqlite/pragma.ex:do_put/4`, `xqlite/pragma.ex:read_without_arg/4`, `xqlite/pragma.ex:resolve_name/1`, `xqlite/pragma.ex:read_pragma/5` | `pragma_test.exs: "an unknown name is refused before any statement reaches SQLite", "the anchor: a number reaches the PRAGMA that reads one", "the anchor: a name no table carries reads empty", "the anchor: a charlist is a list, and nothing is written", "the anchor: nil is not an argument, on either path", "the anchor: nil is no name on any door", "the anchor: true and false are still names on the raw doors", "the anchor: a term that is no keyword list is no options", "the anchor: an unknown key and a db_name that is no name", "memory mapped I/O has no size in memory", "the same PRAGMA answers a number off a file database", "no write door takes the atom back", "the argument of a reading pragma is refused", "a db_name option is refused on both doors", "a pragma name that is not UTF-8 is refused as a name", "the two setters answer their own refusal for a value that is not UTF-8"; nif/pragma_test.exs: "get_pragma/2 answers the first row's first column, not the whole list"` |
| `strict-helpers-resolve-temp-first` | check_strict_violations/2 and enable_strict_table/2 resolve an unqualified name the way SQLite does — the temp schema first, then main, then the attached databases, comparing names with ASCII case folded and no other letter — read the columns from the object that resolved, schema-qualified, refuse anything that is not a plain table ({:not_a_plain_table, %{table, type}} for a view, a virtual table or a shadow table) and a WITHOUT ROWID table, report declared types STRICT would refuse before any row query, return :ok without work on an already-STRICT table, and rebuild inside the schema the table was found in under the spelling stored there (temp.-qualified sources, an unqualified RENAME TO target). | `xqlite.ex:strict_target/2`, `xqlite.ex:resolve_object/2`, `xqlite.ex:convert_to_strict/3` | `strict_table_test.exs: "a temporary table shadowing a main one converts only the temporary one"` |
| `pragma-values-validated-once` | One validator, Xqlite.Pragma.check_value/2, serves Xqlite.set_pragma/3, Xqlite.Pragma.put/4 and the open path: a boolean pragma takes true/false, 1/0 and the words on/off/yes/no/true/false in any case as atoms or strings (canonical 1/0); an integer pragma takes an integer inside the spec's range only, never a boolean; a word pragma takes the spec's words in any case as atoms or strings (canonical upper-case string), and a mapped pragma whose mapping holds true and false (secure_delete) takes the boolean vocabulary on/yes/off/no too, canonicalized to the mapped word, while a mapping without booleans (auto_vacuum, temp_store) refuses them — SQLite would take them silently; synchronous takes off because OFF is one of its own four mode words, and refuses on/yes/no; nil and anything else answer {:invalid_pragma_value, %{pragma, value}}; a read-only pragma answers {:read_only_pragma, atom}; the canonical value is what reaches the NIF, and XqliteNIF.set_pragma/3 stays raw. A spec's words are accepted through its int_mapping in any case and answered as the canonical word, secure_delete's integer domain is 0..1, and each integer range is SQLite's own acceptance domain measured live (max_page_count 1..4294967294, the heap limits 64-bit, threads 0..8, journal_size_limit from -1, mmap_size on the 32-bit range SQLite clamps to); hard_heap_limit is process-wide, applies only when lowering, and is never generated by a test. mmap_size's domain is 0..0x7FFF0000, the bundled build's MAX_MMAP_SIZE (SQLite stores the maximum above it and 0 below zero), pinned against PRAGMA compile_options so a bundled-SQLite bump that moves it fails loudly; UPGRADE_PLAYBOOK.md re-measures it on a bump. | `xqlite/pragma.ex:check_value/2` | `pragma_test.exs: "a word no boolean pragma takes is refused, and the setting stands", "zero and the ceiling are written and read back", "a negative size and one past the ceiling are refused", "the ceiling in the schema is the one this build was compiled with"; pragma_domain_law_test.exs: "the anchor: a mapped PRAGMA holding booleans takes the boolean words", "a mapping without booleans keeps refusing the boolean words"` |
| `child-handle-registration-is-rollback-safe` | Every raw SQLite handle xqlite hands to Elixir is put into the owning connection's child map while that connection's Mutex is held, and a handle whose registration fails is given straight back to SQLite instead of being returned; the two locks are always taken in the same order, the connection Mutex first and the child map second. A statement prepared for a stream lives in an RAII holder inside the with_conn closure that finalizes it on every path that does not register it, so a rejected parameter leaves no statement behind and close stays :ok. | `connection.rs:register_child`, `statement.rs:PreparedStmt` | unpinned |
| `span-events-share-one-shape` | Every span fires three events with fixed measurement keys: :start carries monotonic_time and system_time, :stop and :exception carry duration and monotonic_time, and all three carry the same telemetry_span_context reference. The :stop metadata replaces the :start metadata instead of merging into it, and a span block may return {value, stop_metadata} or {value, extra_measurements, stop_metadata}. | `xqlite/telemetry.ex:run_span/3`, `xqlite/telemetry.ex:emit_stop/4` | `telemetry_test.exs: ":start and :stop share one telemetry_span_context reference"; "a block returning {value, extra_measurements, stop_metadata} merges the extras"` |
| `object-name-errors-carry-the-name` | The four errors that name a database object carry that name, not the sentence around it: {:no_such_table, name}, {:no_such_index, name}, {:table_exists, name} and {:index_exists, name}. The quoting and the schema qualifier are SQLite's own and differ between the pairs — the two 'no such' errors give the resolved name unquoted and keep a qualifier the statement wrote, the two 'already exists' errors never carry a qualifier, :table_exists echoes the identifier exactly as the statement spelled it and :index_exists gives the resolved name. A reworded SQLite message leaves the whole text in the payload. | `error.rs:classify_sqlite_error`, `error.rs:name_after`, `error.rs:name_between` | `nif/execution_test.exs: "execute/3 returns error for NoSuchTable on INSERT"` |
| `object-type-atoms-match-across-the-boundary` | The object types the schema reader reports are one set of atoms on both sides — :table, :view, :shadow, :virtual, :sequence — spelled the same in the Rust atom table and in Xqlite.Schema.Types.object_type/0; a Rust raw identifier gets an explicit string so the atom is not named after its escape. | `lib.rs:atoms` | `schema_introspection_test.exs: "schema_list_objects names a virtual table and its shadow tables"` |
| `callback-sends-run-on-a-dirty-scheduler` | Every enif_send with a NULL calling environment in this crate runs inside a NIF scheduled DirtyIo; nif.rs carries 100 #[rustler::nif] functions, 94 of them scheduled DirtyIo, and the six that carry no schedule (create_cancel_token, cancel_operation, is_cancel_token, sqlite_version, register_log_hook, unregister_log_hook) drive no SQLite callback, so none of them can reach such a send. | `hook_util.rs` | unpinned |
| `blob-wrapper-forces-blob-storage` | A plain binary parameter is stored as TEXT when its bytes are valid UTF-8 and as a BLOB otherwise, while %Xqlite.Blob{bytes: bytes} is always stored as a BLOB; the wrapper is accepted as a positional element and as the value of a keyword pair on every parameter-taking path; bytes is an enforced key (the literal without it does not compile, struct! raises), and a bytes field that is not a binary — a nil built through struct/2 included — answers {:invalid_blob_bytes, %{position, type}} with the one-based position in the list the caller passed and type naming the term found (one of eleven term-type atoms, :bitstring for a bitstring that is not a whole number of bytes, never :binary); and a value read back is a plain binary, never wrapped. | `util.rs:blob_struct_bytes`, `util.rs:blob_struct_value`, `util.rs:elixir_term_to_rusqlite_value` | `nif/blob_param_test.exs: "the same bytes are TEXT plain and BLOB wrapped"` |
| `strict-rebuild-restores-the-two-pragmas` | The STRICT rebuild reads PRAGMA foreign_keys and PRAGMA legacy_alter_table before it starts, turns foreign_keys off before the BEGIN (SQLite ignores that pragma inside a transaction) and legacy_alter_table on just before the RENAME TO, and puts both back on every path, success or failure; it rejects a call inside a caller's transaction with :transaction_in_progress and an existing <table>_xqlite_strict_rebuild in the same schema with {:table_exists, name}, both before any statement runs. | `xqlite.ex:rebuild_pragmas/1`, `xqlite.ex:suspend_foreign_keys/2`, `xqlite.ex:restore_pragmas/3` | `strict_table_test.exs: "a successful rebuild preserves the schema, the rows and the pragmas"` |
| `param-list-shape-decided-by-the-first-element` | Whether a parameter list is positional or a keyword list is decided once, from the first element — a two-element tuple with an atom key means keyword, anything else means positional — and the whole list is then treated that way. | `util.rs:is_keyword` | `nif/blob_param_test.exs: "a positional list whose first element is a wrapper binds every element"; "a named parameter called :blob still binds by name"` |
| `cancel-tokens-validated-before-the-nif` | A :cancel_tokens value — one token or a list — is validated before any cancellable NIF runs: Xqlite.validate_cancel_tokens/1 asks the NIF is_cancel_token/1, which takes the term undecoded and answers true only for a token from create_cancel_token/0, so every Xqlite door that takes tokens (query_cancellable/5, execute_cancellable/5, execute_batch_cancellable/3, query_with_changes_cancellable/5, multi_step_cancellable/3, backup_with_progress/6, stream/4, cancel_operation/1) answers {:error, {:invalid_cancel_tokens, %{reason: :bad_element, position: n, value_type: t}}} for the first element that is no live token (a single term that is no token is position 1) and %{reason: :improper_tail, value_type: t} for a list whose tail is not [], through a hand-written walk (walk_cancel_tokens/2), inside its telemetry span, instead of raising; cancel_operation/1 takes one token and refuses a list, live tokens or empty, as the element at position 1. The raw XqliteNIF functions answer the same map for an element that is no live token, and {:expected_list, %{reason: :not_a_list \| :improper_tail, value_type}} for a term that is no list or a broken tail, while a raw stub still raises for a wrongly typed argument that is not a list (cancel_operation/1 for instance); nil is refused (pass [] for no tokens). The raw NIFs answer {:invalid_cancel_tokens, list_refusal} for EVERY wrong token argument — :not_a_list and :improper_tail as well as :bad_element, cancel.rs:decode_tokens re-tagging the walk's refusal through XqliteError::about_cancel_tokens — so {:expected_list, _} on a cancellable call is always about the parameters; the reason for a bare non-list term differs by door on purpose (raw :not_a_list, Xqlite :bad_element at position 1). | `xqlite.ex:validate_cancel_tokens/1`, `nif.rs:is_cancel_token` | `cancel_token_law_test.exs: "the anchor: a stream refuses a reference that is not a token", "the anchor: the refusal names the position of the element", "the anchor: a token list that does not end in [] is refused", "the anchor: a raw door names the position too", "the anchor: cancel_operation/1 takes one token, not a list", "the anchor: a raw door names a token argument that is no list", "the anchor: a parameter list that is no list is still about the parameters"` |
| `bad-input-raises-or-answers` | An argument of the wrong type raises where it is met — a guard in Xqlite or in Xqlite.Pragma, or the native argument decoding inside a raw XqliteNIF stub — while a value of the right type that the library or SQLite refuses comes back as an {:error, reason} answer; the pragma doors judge every argument this way except the connection, which raises. Three exceptions, all stated in XqliteNIF's moduledoc: an argument the library reads as a list (the parameter lists, the cancel-token lists, the authorizer's action list) is never handed to rustler's decoder and answers {:expected_list, map}, {:expected_keyword_list, map} or {:invalid_cancel_tokens, list_refusal} instead of raising, on the raw stubs as on the Xqlite doors; a PRAGMA name on the raw doors is judged as bytes and answers {:invalid_pragma_name, name} whatever kind of term it was; the stream fetch doors judge their batch size and answer {:invalid_batch_size, %{provided, minimum: 1}} for a wrong type. XqliteNIF's moduledoc states the rule: the wrong TYPE raises (ArgumentError on a raw stub, FunctionClauseError at a guarded Xqlite function), a value of the right type the library refuses answers — a binary that is not UTF-8 where text is meant included, :invalid_utf8_in_string. The parameter argument of Xqlite.bind/2,3 is one of the lists the library reads: it answers {:expected_list, _} for a term that is no list, never raises. | `xqlite.ex:@moduledoc`, `xqlite/pragma.ex:@moduledoc` | `bad_input_answers_test.exs: "a guard on an Xqlite function raises", "the native argument decoding raises", "a value of the right type is an answer", "a pragma door raises only for a connection that is not one", "a raw NIF stub raises on a term it cannot decode"` |
| `readme-counts-come-from-the-code` | The four counted numbers in README.md are read off the code, not written as prose: the count of typed error reasons is the size of Xqlite.error_reason/0, the count of constraint subtypes is the size of Xqlite.constraint_kind/0 minus its fallback, the two PRAGMA counts are the size of Xqlite.Pragma's schema and of its writable half, and the count of built-in type extensions is the number of modules under lib/xqlite/type_extension/. | `readme_census_test.exs` | `readme_census_test.exs: "the README's count of typed error reasons is the union's size", "the README's count of constraint subtypes is the union minus its fallback", "the README's PRAGMA counts are the schema's size and its writable half", "the README's count of built-in type extensions is the number of modules"` |
| `caller-lists-are-walked-by-hand` | No rustler list decoder ever touches a caller's term: every list a NIF takes (parameters on query, execute, query_with_changes, stream_open, stmt_bind and explain_analyze, the cancel-token lists on the cancellable NIFs, the authorizer's denied actions) is walked once by util.rs's own walker over enif_get_list_cell, which refuses a non-list tail with a structured error instead of the panic rustler's ListIterator raises; the walked cells feed both the first-element keyword rule and the element decode; an improper list therefore answers {:expected_list, %{reason, value_type}} (or the keyword twin) on every door and never aborts the VM. lib/xqlite.ex:params_count/1, the telemetry count, walks a parameter list by hand and stops at a tail that is not a list so the NIF names the tail. A token list's refusal is re-tagged {:invalid_cancel_tokens, …} by cancel.rs:decode_tokens. The :type_extensions option is walked by hand — Xqlite.TypeExtension's own walk — on every Xqlite door that takes it and by Xqlite.TypeExtension.encode_params/2 and decode_rows/2 themselves (nil and an absent option mean none; a proper list of extension modules passes — an atom naming a module that Code.ensure_loaded?/1 finds (the check loads it), that declares @behaviour Xqlite.TypeExtension and that exports both encode/1 and decode/1, on every door whether or not it decodes; a module that passed is remembered for the life of the node in a persistent term keyed {Xqlite.TypeExtension, module} for its behaviour declaration only: on every later call the walk asks function_exported?/3 for both callbacks and runs the full check again when either is missing, so a module merely unloaded reloads and passes, a module recompiled without a callback or purged and no longer loadable is refused, and one recompiled without @behaviour but with both callbacks keeps passing; a refused module is asked again on the next call — and an atom that is no such module answers {:invalid_type_extensions, %{reason: :bad_element, position, value_type: :atom}} at its position, as any other element that does not belong; anything that is no proper list answers {:invalid_type_extensions, list_refusal}; all of it before any metadata or NIF call). On the three cancellable Xqlite doors the option is judged before the cancel tokens, and it is the one refusal those doors answer outside their telemetry span: no event is emitted for it. The encode chain walks the parameter list's spine and the arity of each keyword pair before the NIF — on the public Xqlite.TypeExtension.encode_params/2 whatever the extension list, on the doors only with an extension on — and its refusals for a broken list are the NIF's own shapes; a keyword element that is no pair travels on and the NIF refuses it. decode_rows/2 walks the outer spine and each row's kind on every call and a row's own tail when an extension walk runs, answering {:expected_list, map} instead of raising. | `xqlite/type_extension.ex:validate_extensions/1`, `xqlite/type_extension.ex:walk_extensions/3`, `xqlite/type_extension.ex:extension_module?/1`, `util.rs:walk_list`, `util.rs:walk_params` | `improper_list_law_test.exs: "every list-taking door refuses an improper list", "the anchor: a stream refuses an improper parameter list at open", "the anchor: a proper list of the same values still streams"; type_extension_test.exs: "a term that is no list at all is refused", "a list whose tail is not one is refused", "an element that is no module name is refused by its position", "nil and an absent option both mean no extensions", "every door that takes the option judges it the same way", "an atom that names no module is refused by its position", "the first element that is no extension is the one reported", "nil and false as elements are refused, unlike the option's own nil", "a module with both callbacks but no declaration is refused", "a module that declares the behaviour but has no decode/1 is refused", "a module that passed is remembered for the node, a refused one is not", "a module that lost a callback, or that can no longer be loaded, is refused", "an empty list still means no extensions on both", "a module with both callbacks but no declaration is refused by both", "an atom that names no module is refused by both, with its position", "a list whose tail is not one is refused by both", "a term that is no list at all is refused by both", "the list is judged before any value is touched"` |
| `nif-panics-unwind-and-are-caught` | The crate is built with panic = unwind, pinned in its own .cargo/config.toml for release and dev because a config file outranks the manifest and a machine-wide panic = abort would silently disarm rustler's guard; with unwinding, rustler catches a panic in a NIF body and in argument decoding and raises :nif_panicked in the calling process, the VM stays up, and a panic while a connection lock is held still poisons that lock for good. scripts/panic_strategy.exs reads the libraries named on its command line by family — an ELF or Mach-O library for _Unwind_RaiseException among its undefined symbols (nm -u or llvm-nm), a Windows .dll for _CxxThrowException or __CxxFrameHandler3 in its import table (objdump -p or llvm-objdump), the tools looked for on PATH and in the rustup sysroot, XQLITE_SYMBOL_TOOL overriding either; mix verify hands it priv/native/xqlitenif.{so,dll} (the file both mix environments load) and the step fails closed when no tool can read the family; the release workflow reads each built library the same two ways into its job summary, naming the tools and the library and saying when the readout was empty, report-only until the marker per target family is measured. | `native/xqlitenif/.cargo/config.toml`, `scripts/panic_strategy.exs` | `panic_strategy_script_test.exs: "the library the virtual machine loaded passes, and is named", "a symbol tool that is not on the machine fails the check", "a missing tool for the Windows library family names that family's tools", "a symbol tool that reads nothing useful fails the check, and is named"` |
| `elixir-list-doors-walk-by-hand` | An Elixir door that walks a caller's list walks it with its own recursive function whose last clause takes a tail that is not a list, so a list ending in something other than [] comes back as the door's own structured refusal — {:invalid_open_option, %{key: nil, reason: :not_a_pair, value: tail}} for open options and {:invalid_cancel_tokens, %{reason: :improper_tail, value_type: type}} for cancel tokens — instead of raising from Enum. A third walker of the same shape, lib/xqlite/type_extension.ex:walk_extensions/3 (reached from Xqlite.type_extensions/1 and both chain functions), judges the :type_extensions option and answers {:invalid_type_extensions, %{reason: :improper_tail, value_type}} for a tail that is no list. The encode chain (Xqlite.TypeExtension.encode_positional/4, encode_keyword/4, encode_params_checked/2) walks the parameter list by hand too and answers what the native walk answers for the same term — {:expected_list, %{reason: :improper_tail \| :not_a_list, value_type}}, {:expected_keyword_list, %{reason: :improper_tail, value_type}} — so a call refuses the same way with extensions on as without; a keyword element that is no pair travels on to the NIF, which answers {:expected_keyword_tuple, %{reason: :bad_element, position, value_type}}. | `xqlite.ex:open_opts_fault/2`, `xqlite.ex:walk_cancel_tokens/2`, `xqlite/type_extension.ex:walk_extensions/3` | `bad_input_answers_test.exs: "an options list that does not end in [] is an answer"; cancel_token_law_test.exs: "the anchor: a token list that does not end in [] is refused"; type_extension_test.exs: "a positional list whose tail is not one is refused either way", "the tail's own kind is what the refusal names", "a keyword list whose tail is not one is refused either way", "a keyword element that is no pair is refused where it always was", "a term that is no list at all is refused either way"` |
| `one-list-walk-per-call` | A caller's list is walked exactly once per call: walk_params reads the first cons cell for the keyword decision and then collects every element, and the decoders take that collected slice (&[Term]) rather than the original term, so no door reads the same list twice. | `util.rs:walk_params` | unpinned |
| `nil-means-no-parameters` | A parameter term of nil means zero parameters on every raw door that takes them (query, execute, query_with_changes, stream_open, stmt_bind, explain_analyze): util.rs:walk_params answers Params::Empty for nil, the one producer, and the empty arm runs the same count check as a list, so nil and [] pass only on a statement that takes none; Xqlite.bind/2,3 carries no guard on the term, so nil means no parameters there as on every other door and a term that is no list answers {:expected_list, %{reason: :not_a_list, value_type}}. | `util.rs:walk_params` | `improper_list_law_test.exs: "nil binds nothing on every parameter door", "nil on a one-parameter statement is a count mismatch"; improper_list_law_test.exs: "a term that is no list at all is refused as such"` |
| `statement-value-error-reports-once` | A row SQLite handed back that xqlite cannot read (invalid UTF-8 in a TEXT column) is reported once, for that row, which is never delivered: step/1 reports it at once; stmt_multi_step and its cancellable twin first hand back the rows read before it in the same batch with done: false and hold the error in the statement's pending-error slot (a Mutex<Option<XqliteError>>, written and taken with the connection Mutex held on the step, multi_step and reset paths and taken before that Mutex on the finalize path, poison recovered); the next call that reads a row — stmt_step, stmt_multi_step or its twin — answers and clears it; reset and finalize drop it; when no row was read before it in the batch the error is answered at once; every door then continues at the row after the bad one. A sqlite3_step that fails outright (stream.rs:StepFailure::Failed — busy, I/O, a runtime error in the SQL, a trigger's RAISE, a cancellation) is answered at once, discards the batch's rows and holds nothing: the statement is left where SQLite leaves it and the next step is SQLite's own rerun from the top, which stmt_reset changes nothing about. The stream twin holds back an unreadable row and a failed step alike — except a cancellation, which it answers at once with the batch's rows discarded — finalizing its statement first, and is finished after reporting. | `nif.rs:stmt_multi_step_impl`, `nif.rs:stmt_step`, `statement.rs:XqliteStatement::store_pending_error`, `stream.rs:process_single_step` | `statement_value_error_test.exs: "the rows read before the bad one come back before the error", "step/1 answers the error a batch held back, then carries on", "a reset drops the error that was held back", "the anchor: a runtime error in the SQL is answered with none of the batch's rows", "a failed step leaves the statement where SQLite left it", "a trigger abort answers the error on every step and never a row"; nif/stream_test.exs: "isolated: a stream that errored delivers no row after the bad one"` |
| `term-kind-names-are-one-vocabulary` | The name a refusal gives a term's kind is one vocabulary — :atom, :binary, :float, :function, :integer, :list, :map, :pid, :port, :reference, :tuple and :unknown — written out once in Rust for the refusal maps (error.rs:term_type_to_atom) and once in Elixir for the refusals the Elixir doors build themselves (lib/xqlite.ex:term_type/1), so a caller sees the same word whichever side of the door refused; the one deliberate exception is {:unsupported_data_type, :bitstring}, about a term that is not a whole number of bytes. Xqlite.term_type/1 is total over the eleven BEAM term kinds by its guards alone and never answers :unknown, which only the Rust twin (error.rs:term_type_to_atom) can answer for a term rustler cannot classify. | `error.rs:term_type_to_atom` | `cancel_token_law_test.exs: "every door names the position and kind of the element that is no token"` |
| `parameter-lists-cover-the-statement` | A parameter list must fit the statement, on every door, before any value is bound. A positional list whose length is not the statement's own parameter count is refused with {:invalid_parameter_count, %{expected, provided}} — query.rs:require_parameter_count ahead of rusqlite's checked binding for query, execute and query_with_changes and their cancellable twins, stream.rs:require_parameter_count for the raw-FFI binds of stmt_bind, stream_open and explain_analyze, the empty list and nil counting as zero; provided is the list's own length everywhere. A keyword list must cover the statement once: every key is resolved to an index first (a key the statement lacks answers {:invalid_parameter_name, name}; a key is used as written when its text starts with ':', '@' or '$' and prefixed with ':' otherwise), two keys resolving to one index answer {:duplicate_parameter_name, name}, and an index no key reached answers {:missing_parameter, %{index, name}} for the lowest such index, name being SQLite's spelling or nil for a bare ? — query.rs:require_named_parameters_covered and stream.rs:require_named_parameters_covered walking sqlite3_bind_parameter_name; SQLite names a ?NNN parameter ?NNN and only a bare ? has no name, and no keyword key reaches either because every key comes out carrying one of the three prefixes ([{:"?1", 1}] asks for :?1), so a statement holding ?NNN or ? refuses a keyword list. SQLite itself refuses none of these shapes: an unbound parameter reads as NULL. One call binds one complete list: partial keyword binds no longer accumulate on bind/3 and stmt_bind/2. :invalid_parameter_name and :duplicate_parameter_name carry the resolved key (a key used as written keeps its prefix; every other key gets ':'); :missing_parameter carries SQLite's own spelling of the parameter no key named, read from the statement. A keyword list is accepted on a statement of at most MAX_NAMED_PARAMETERS (util.rs, 2 048) parameters and refused above it by util.rs:decode_exec_keyword_params before any element is decoded, with {:too_many_named_parameters, %{count, limit}}, count being the statement's parameter count, on every door of both families; a positional list has no cap; the names of an accepted list resolve through one map built once per call from sqlite3_bind_parameter_name over 1..=count (a bare ? has no name and is skipped), one lookup per key (query.rs:parameter_indices_by_name, stream.rs:parameter_indices_by_name), about 6.4 ms at 2 048 parameters on stmt_bind/2 and query/3, since reading one name is itself a walk of SQLite's parameter list (sqlite3VListNumToName) and the map grows faster than the count, the reason for the cap. The order: too many named parameters, the element and value errors of the decode, an unknown key, a duplicate key, a parameter no key named, the length check; the claimed set is a flag vector sized from sqlite3_bind_parameter_count (capped by SQLite at 32 766); both walks treat a parameter index of 0 as no such parameter by one rule; the raw walk returns the per-key index list in caller order, and the rusqlite doors bind a keyword list by index through rusqlite's raw binding with those indices. | `util.rs:decode_exec_keyword_params`, `stream.rs:require_parameter_count`, `query.rs:require_parameter_count`, `stream.rs:require_named_parameters_covered`, `query.rs:require_named_parameters_covered`, `query.rs:parameter_indices_by_name`, `stream.rs:parameter_indices_by_name`, `query.rs:bind_named_by_index` | `parameter_coverage_law_test.exs: "a keyword list is taken up to 2 048 parameters and refused above, on both families", "the anchor: a list one element short through a stream writes nothing", "an empty list on a statement that takes one parameter is refused everywhere", "a list two elements too long is refused, counted the same on every door", "the anchor: a keyword list that leaves a name out writes nothing", "a key that names the same parameter twice is refused", "the same name twice in the SQL is one parameter with one value", "a parameter no keyword list can name is refused by its index", "a key already carrying a name prefix is used as written", "a key the statement lacks is refused before a name that is missing", "a keyword list in any order binds each value to its own parameter"; nif/statement_test.exs: "two partial keyword binds do not add up"; parameter_coverage_law_test.exs: "a keyword list is refused by the same rule however few keys it holds"` |
| `blob-read-is-a-window-write-is-not` | blob_read/3 answers the bytes that are in range and no error for a short or empty range — fewer bytes than asked for when the blob ends first, {:ok, ""} when offset is at or past the end or length is zero, without calling SQLite — while blob_write/3 refuses a write that would run past the end with {:error, {:cannot_execute, reason}} instead of writing the part that fits; a negative offset or length raises on both, the arguments being usize on the native side. | `blob.rs:read`, `blob.rs:write` | `nif/blob_test.exs: "a read is a window over the bytes that are there"` |
| `statement-steps-only-when-bound` | A prepared statement with one or more parameters refuses to step until a bind has succeeded since prepare: the resource carries a flag, false at prepare for such a statement (true for one with none), set true by a successful bind and by clear_bindings (the one explicit way to run with NULLs; a clear on a statement that takes parameters and is mid-run — sqlite3_stmt_busy: a step has run and neither done nor reset followed — is refused with the bare atom :statement_mid_run inside the connection lock and changes nothing, since SQLite's own clear has no mid-run check and would turn every remaining row's parameter read into NULL; a statement that takes none is cleared mid-run as it is bound with [] mid-run, both :ok; after done a clear is allowed and a bind is SQLite's misuse refusal), left unchanged by a bind the library refused — the count, the names, a value that does not convert, a value over the connection's length limit — which binds nothing (an earlier successful bind stays in force), and cleared by a bind that failed after at least one value was taken — SQLite's misuse refusal of a bind on a statement mid-run comes before the first parameter is touched and so binds nothing and leaves the flag alone; an allocation failure after a value was taken is the one way in — which leaves the statement unrunnable until a bind succeeds or clear_bindings runs and by reset (SQLite keeps bindings across a reset); stmt_step, stmt_multi_step and its cancellable twin answer {:parameters_unbound, %{expected: n}} for a false flag, behind the finalized and closed checks; expected is read once at prepare with sqlite3_bind_parameter_count. SQLite itself runs an unbound parameter as NULL. The flag is set and cleared inside the closure that holds the connection lock, on the same path as the bind, so a step that took the lock after a bind answered :ok never reads it unset. | `nif.rs:stmt_prepare`, `nif.rs:stmt_bind`, `statement.rs:XqliteStatement`, `nif.rs:stmt_clear_bindings`, `statement.rs:mark_parameters_set`, `statement.rs:require_parameters_set`, `stream.rs:BindFailure`, `statement.rs:clear_parameters_set`, `limits.rs:require_positional_within_length`, `limits.rs:require_named_within_length` | `nif/statement_test.exs: "stepping before any bind is refused", "a refused bind leaves nothing to run", "a closed connection is reported before an unbound statement"; bind_before_step_law_test.exs: "the anchor: a statement that takes a parameter is refused", "the anchor: a statement that takes none runs at once", "clear_bindings is how a caller asks for NULLs", "a refused bind leaves an earlier successful one in force", "an UPDATE refused at the step writes nothing"; nif/statement_test.exs: "a value over the connection's length limit binds nothing", "a keyword list over the length limit binds nothing either"; bind_before_step_law_test.exs: "a bind refused for its length leaves an earlier one in force"; nif/statement_test.exs: "a bind SQLite refuses mid-run takes no value and retires nothing", "a keyword bind SQLite refuses mid-run takes no value either"` |
| `stream-default-batch-size-is-one-constant` | The stream's default batch size is written once, as Xqlite.StreamResourceCallbacks' @default_batch_size, and read everywhere else through default_batch_size/0 — Xqlite.stream/4's runtime default and its own documentation included, the doc interpolating the function at compile time — so the documented default and the applied default cannot drift apart. | `xqlite/stream_resource_callbacks.ex:@default_batch_size` | unpinned |
| `bind-failure-split-is-only-for-the-statement-resource` | The two raw binders report whether SQLite had already taken a value before a failure (stream.rs:BindFailure, NothingBound against PartlyBound: a failure on the first value that is SQLite's misuse, code 21, took nothing; every other failure has replaced that parameter with NULL, so stream.rs:bind_failure tags by position and code), and only nif.rs:stmt_bind acts on the split — clearing the parameters-set flag on PartlyBound — because a prepared-statement resource is the only thing that outlives a failed bind: stream_open and explain_analyze hold their statement in PreparedStmt, which finalizes it on every path out of the closure, so both collapse the two cases into the plain error with BindFailure::into_error. A door that binds into a statement surviving the call must act on the split, or a half-bound statement becomes steppable again. | `stream.rs:BindFailure`, `stream.rs:bind_positional_params_ffi`, `stream.rs:bind_named_params_ffi`, `stream.rs:bind_failure` | `nif/statement_test.exs: "a bind SQLite refuses mid-run takes no value and retires nothing", "a keyword bind SQLite refuses mid-run takes no value either"; bind_before_step_law_test.exs: the :bind_mid_step action of "a step answers the rule whatever came before it"` |
| `stream-rows-are-maps-over-unique-names` | Xqlite.stream/4 answers each row as a map keyed by column name, so it refuses at open, after stream_open and stream_get_columns and before any row is fetched, a statement whose column names repeat — {:error, {:duplicate_column_name, name}}, name the name at the first position that repeats an earlier column, in SQLite's order — SELECT 1 AS a, 2 AS b, 3 AS b, 4 AS a answers b — (a column with no name of its own is named after its text, so SELECT 1, 1 and SELECT ?, ? repeat too); the law's oracle is a brute-force reading independent of the code — closing the handle it was handed on that path; the check compares the count of distinct names with the column count and walks for the first repeat only when they differ. A caller aliases the columns. The raw stream doors and query/4 answer lists and take any names, and stream_get_columns/1 answers the full list in SQLite's order. | `xqlite/stream_resource_callbacks.ex:judge_columns/5`, `xqlite/stream_resource_callbacks.ex:repeated_column/1`, `xqlite/stream_resource_callbacks.ex:map_rows_to_maps/3`, `xqlite/stream_resource_callbacks.ex:first_repeat/2` | `stream_column_name_law_test.exs: "the anchor: a join of two tables sharing a column name is refused at open", "the same join with aliases streams both values", "a statement whose names are all different streams as it always did", "an expression written twice repeats the name SQLite gives it", "the refusal names the column at the first repeating position", "the doors that answer lists keep both values", "a refused open leaves the connection ready for the next stream"; xqlite_test.exs: "stream/4 rejects a statement whose column names repeat at open", "stream/4 takes the same columns once they are aliased"` |
| `connection-limits-are-sqlites-own` | A limit is read with Xqlite.get_limit/2 (XqliteNIF.get_limit/2) and written with Xqlite.put_limit/3 (XqliteNIF.put_limit/3), each one sqlite3_limit call under the connection lock; a write answers the value now in force, read back with sqlite3_limit(id, -1) in the same lock hold, after SQLite's own clamping (its compile-time ceiling per category, 30 as the floor of :length); the library adds no floor and no ceiling. The value is judged before the category: an integer outside 0..2_147_483_647 answers {:invalid_limit_value, %{category, value}} — at the Xqlite.put_limit/3 guard for every integer, bignums included, and in limits.rs:judge_value on the raw NIF — and an atom naming none of the thirteen categories answers {:invalid_limit_category, atom} on both functions. A term of the wrong kind raises: FunctionClauseError at the Xqlite guards, ArgumentError at the NIF's decoder, which also raises for an integer outside signed 64 bits on the raw stub. A :length lowered below values already stored makes their reads answer SQLite's own refusal at the step; the length pre-check of every bind reads this limit; the no-statement tell of the rusqlite doors reads the expansion with the limit lifted and restored, so no statement is hidden by a lowered limit; XqliteNIF.get_create_sql/2 judges its object name against the limit like a bound value (limits.rs:require_text_within_length). | `limits.rs:in_force`, `limits.rs:judge_value`, `limits.rs:category_id`, `nif.rs:get_limit`, `nif.rs:put_limit`, `xqlite.ex:put_limit/3` | `connection_limit_law_test.exs: "the anchor: every category reads a value of its own", "the anchor: a write answers the value now in force, the floor included", "the anchor: a value outside the domain is refused before the category", "an atom naming no category is refused", "a term of the wrong kind is refused at the typed door"; value_length_limit_law_test.exs: "the name an introspection door looks up is judged like a bound value"` |
| `type-extension-callbacks-answer-three-shapes` | encode/1 and decode/1 answer {:ok, value}, :skip or {:error, reason}; any other answer is refused by the function that called the callback with reason: {:bad_return, answer}, never a raise. An encode refusal is {:type_extension_refused, %{position, extension, reason}} before any SQL runs; a decode refusal is {:type_extension_refused, %{column, extension, reason}}, column the value's one-based place in its row, answered by query/4, query_cancellable/5 and query_with_changes_cancellable/5 inside their telemetry span after the statement ran (its changes stand), by decode_rows/2, and by stream/4 after the rows decoded before it, under its :on_error mode (kept in the accumulator's decode_error until the next pull, which fetches nothing and emits no fetch event). encode_value/2 and decode_value/2 answer {:ok, value} or {:error, %{extension, reason}}. | `xqlite/type_extension.ex:encode_value/2`, `xqlite/type_extension.ex:decode_value/2`, `xqlite/type_extension.ex:decode_row_checked/2`, `xqlite/stream_resource_callbacks.ex:next_fun/1` | `type_extension_decode_law_test.exs: "the anchor: a value refused in the second of three columns", "under :emit_error the rows before the refused one arrive, then the refusal", "under :halt the rows before the refused one arrive, then the stream stops", "under :raise the rows before the refused one arrive, then the raise", "a callback answer outside the three shapes is a bad return, on both sides", "a value an extension decodes to an error tuple is data, not a refusal", "a refusal comes after the statement ran: its changes stand, the stop event says so"` |

## 5. Build facts

- **SQLite compile flags.** `native/xqlitenif/.cargo/config.toml` sets
  `LIBSQLITE3_FLAGS = "-DSQLITE_ENABLE_STMT_SCANSTATUS=1"`, without which
  `sqlite3_stmt_scanstatus_v2` returns `SQLITE_MISUSE` and
  `Xqlite.explain_analyze/3` has nothing to report. Everything else comes
  from `libsqlite3-sys`'s bundled build, which always passes
  `-DSQLITE_ENABLE_API_ARMOR`, `-DSQLITE_THREADSAFE=1`,
  `-DSQLITE_DEFAULT_FOREIGN_KEYS=1`, `-DSQLITE_ENABLE_FTS5`,
  `-DSQLITE_ENABLE_RTREE`, `-DSQLITE_ENABLE_STAT4`,
  `-DSQLITE_ENABLE_COLUMN_METADATA`, `-DSQLITE_ENABLE_DBSTAT_VTAB` and
  more; the `session` cargo feature adds `-DSQLITE_ENABLE_SESSION` and
  `-DSQLITE_ENABLE_PREUPDATE_HOOK`. Nothing here can turn API_ARMOR off.
- **Cargo must run from the crate directory,** because it finds
  `.cargo/config.toml` only by walking up from its working directory;
  `--manifest-path` from the repo root loses `LIBSQLITE3_FLAGS`, so
  `lib/mix/tasks/verify.ex:run_cargo/1` sets `cd: "native/xqlitenif"`.
- **Threading.** `SQLITE_THREADSAFE=1` is the compiled mode, but rusqlite
  opens every connection with `SQLITE_OPEN_NO_MUTEX`, so the Rust
  `Mutex<Connection>` is the only serialization. The two are
  complementary: `rusqlite::Connection` is `!Sync`, so the Rust `Mutex`
  is required by the type system anyway, and NO_MUTEX is safe *because*
  it is there. `open_readonly` and `open_in_memory_readonly` pass
  NO_MUTEX explicitly; the rest inherit it from `OpenFlags::default()`.
- **Rustler and NIF versions.** `Cargo.toml` declares the cargo features
  `nif_version_2_15` / `2_16` / `2_17`, defaulting to `2_17`, and sets
  `lto = true` with `codegen-units = 1` for release builds; the Rust
  edition and toolchain floor live there too.
  `lib/xqlite/xqlitenif.ex` declares `nif_versions: ["2.17"]` and the
  eight precompiled targets, and drives `force_build:` off the
  `XQLITE_BUILD` environment variable.
- **Telemetry flag.** `lib/xqlite/telemetry.ex` reads
  `Application.compile_env(:xqlite, :telemetry_enabled, false)` — a
  compile-time value, so changing it needs
  `mix deps.compile xqlite --force`. **Warnings as errors:** `mix.exs`
  sets `elixirc_options: [warnings_as_errors: true]` for `lib/`, and the
  `test:` alias appends `--warnings-as-errors`, which `mix test.seq`
  inherits when it shells out to `mix test` per file.
